[package]
name = "tsap_tests"
version = "0.0.0"
authors = ["Lorenz Schmidt <bytesnake@mailbox.org>"]
edition = "2021"
publish = false

[dependencies]
tsap = { path = "../tsap" }

[dev-dependencies]
macrotest = "=1.0.9"
trybuild = "1.0"
